import React, {Component} from 'react';
import './board.css';
import Nav from './nav'

export default class Board extends Component {
    render() {
        return (
            <Nav />
        );
    }
}